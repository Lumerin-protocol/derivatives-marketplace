name: Keeper E2E Tests (deprecated)

# Legacy keeper tests for the derivatives-marketplace/keeper package.
# Active keeper code and CI live in Lumerin-protocol/collateral-margin.

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Keeper E2E for this repo is retired. See collateral-margin/keeper and keeper-test.yml."
          exit 1
